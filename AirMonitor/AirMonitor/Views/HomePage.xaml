<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AirMonitor.Views.HomePage"
             xmlns:models="clr-namespace:AirMonitor.Classes"
             Title="Home Page">
    <Grid>
        <ListView ItemsSource="{ Binding Measurments }" ItemTapped="Select">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{ Binding Installation.Address.Street }" Detail="{ Binding Installation.Address.City }" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <ActivityIndicator IsRunning="{ Binding IsLoading }" Color="Teal" Scale="0.5" />
    </Grid>
</ContentPage>